apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat-microservice
spec:
  replicas: 1 
  selector:
    matchLabels:
      app: recordhub
  template:
    metadata:
      labels:
        app: recordhub
    spec:
      containers:
      - name: chat-microservice
        image: skava600/recordhub-chat:latest
        ports:
          - containerPort: 5009
        env:
          - name: "ASPNETCORE_ENVIRONMENT"
            value: "Production"
        volumeMounts:
          - name: secrets
            mountPath: /app/appsettings.Production.json
            readOnly: true
            subPath: appsettings.Production.json
      volumes:
      - name: secrets
        secret:
          secretName: chat-secret